# binaries (root-anchored so they don't match cmd/distill* source dirs)
/distill
/distill-server
/distilld
*.exe
dist/
# knowledge bases / scratch
.knowledge/
*.sqlite
*.sqlite-wal
*.sqlite-shm
# coverage
coverage.txt
